# Determine the install dir
if [ -z $1 ]; then
    INSTALL_DIR="$HOME/jg_dev_env"
else
    INSTALL_DIR=$1
fi

mkdir -p $INSTALL_DIR
echo Install Dir: $INSTALL_DIR

docker run -it -v $INSTALL_DIR:/jg_dev_env -w /jg_dev_env centos:7
